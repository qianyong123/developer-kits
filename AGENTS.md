# developer-kits 项目约定

## 图片识别

- 本项目的图片识别、图片分析、OCR 统一使用 **zhipu-vision** MCP（智谱视觉模型），不要使用其他图片识别方案。
- 使用入口：zhipu-vision 提供 `analyze_image` / `extract_text` 等工具。
- MCP 配置在 `.codex/config.toml`（本地配置，已被 `.gitignore` 忽略，不随仓库提交）。

## 项目简介

- 前端开发工具箱（React + Vite + TypeScript），当前包含图片压缩工具。
- 图片压缩目标：默认有损压缩、保真优先（肉眼几乎看不出差异）、支持批量格式转换、质量/压缩比例档位选择。

## 提交流程约定

- 每完成一个功能的代码或修复一个问题后，先自测（单元测试 / lint / 构建 / e2e，按改动范围选择），确认通过后再提交。
- 修复/改动后要检查是否影响其他页面或功能（搜索相关引用、运行完整测试）；能自动修复的直接影响直接修复，影响面较大时先告知用户再处理。
- 提交先只保存到**本地**（`git commit`），不要主动推送到远程。
- 同一个功能的多次修改（实现、微调、修复）允许产生多个本地提交；当用户说「推送远程」时，先把**同一功能的多个提交合并成一个**（squash），再推送。
- 不同功能的修改保持独立提交，不要混在一起。
- 推送前先向用户确认，再执行 `git push`。

## 前端开发规范（React + Vite + TypeScript）

### TypeScript

- 开启 `strict`；禁止无类型 `any`，确需动态类型时用 `unknown` + 类型收窄，并加注释说明原因。
- 类型先行：业务对象、组件 props 显式定义并导出复用；状态用判别联合（discriminated union）表达，避免用多个 boolean 组合出非法状态。

### 组件与逻辑

- 函数组件 + hooks；单文件接近 400~500 行时主动拆分：视图组件、自定义 hook（`useXxx`）、纯函数、常量分层。
- 页面组件只做状态编排与 JSX 拼装，业务逻辑尽量抽成自定义 hook 或 lib 纯函数。
- `useMemo` / `useCallback` / `memo` 按需使用：避免内联对象/函数造成无意义重渲染，但不要为用而用。

### 状态管理

- 状态就近存放，派生状态用选择器或 `useMemo`，不重复保存冗余状态。
- 跨组件共享状态用轻量方案（本项目 zustand），store 保持小而单一职责，避免过度 prop drilling。

### 样式

- 使用 CSS Modules + CSS 变量（设计令牌），颜色、圆角、阴影等统一走 `global.css` 的 `--*` 变量，不在组件里写魔法值。
- 深浅色主题都要检查（`data-theme='dark'` 下同样可读）。
- 响应式断点与现有布局保持一致。

### Vite / 构建

- 路径别名统一用 `@/`；路由懒加载、大依赖按需分包。
- 构建保证 `tsc --noEmit` 通过；关注产物体积与重复依赖。

### 测试与质量

- 纯函数/算法写 vitest 单测；关键用户流程写 Playwright e2e。
- 改动后按范围跑 lint / 单测 / 构建 / e2e，全部通过再提交，并遵守上方「提交流程约定」。
