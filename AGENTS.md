# developer‑kits 项目约定

## 常驻规则
- 【精简常驻规则】完整详细编码规范见 skill：`frontend-quality`。
- ⚠️ 执行组件编写、逻辑开发、重构、代码审查等任务时，自动调用 `$frontend-quality` 启用完整规范。

## 图片识别
- 本项目的图片识别、图片分析、OCR、截图解析**统一使用 zhipu‑vision MCP（智谱视觉模型），禁止使用其他图片识别方案**。
- 只要任务涉及图片解析、OCR读取图片文本、识别截图内容，优先调用 zhipu‑vision MCP。
- 使用入口：zhipu‑vision 提供 `analyze_image` / `extract_text` 等工具。
- MCP 配置在 `.codex/config.toml`（本地配置，已被 `.gitignore` 忽略，不随仓库提交）。

## 项目简介
- 前端开发工具箱（React + Vite + TypeScript），当前包含图片压缩工具。
- 图片压缩目标：默认有损压缩、保真优先（肉眼几乎看不出差异）、支持批量格式转换、质量/压缩比例档位选择。

## 提交流程约定
- 每完成一个功能的代码或修复一个问题后，先自测（单元测试 / lint / 构建 / e2e，按改动范围选择），确认通过后再提交。
- 修复/改动后要检查是否影响其他页面或功能（搜索相关引用、运行完整测试）；能自动修复的直接影响直接修复；**改动影响面较大时，先输出改动方案告知用户，得到确认再编码**。
- 提交只允许保存到**本地**（`git commit`），**禁止主动推送到远程仓库**。
- 同一个功能的多次修改（实现、微调、修复）允许产生多个本地提交；当用户明确说「推送远程」时，**必须将同一功能的多个提交squash合并为单个提交**，再执行推送。
- 不同功能的修改保持独立提交，禁止把多个无关需求混在同一个commit。
- 推送前必须向用户确认，再执行 `git push`。

## 前端开发规范（React + Vite + TypeScript）

### TypeScript
- tsconfig开启 `strict`；**尽量禁止 any**；确需动态类型必须优先使用 `unknown`，做类型收窄，并且增加注释说明为什么无法推导类型。
- 业务对象、组件 props 显式定义并导出复用；状态优先使用判别联合(discriminated union)，避免多个boolean组合产生非法状态。

### 组件与逻辑
- 使用函数组件 + hooks；单文件代码接近 400‑500 行时，**先告知用户，再进行拆分**；拆分为视图组件、自定义 hook（`useXxx`）、纯函数、常量分层。
- 页面组件只负责状态编排与JSX拼装；业务逻辑抽离到自定义hook或者lib纯函数。
- `useMemo` / `useCallback` / `memo` 按需使用：解决真实重渲染问题，禁止为了加而加。

### 状态管理
- 状态就近存放；派生状态优先使用选择器或 `useMemo`，不保存冗余重复状态。
- 跨组件共享状态使用 zustand；store保持单一职责，避免prop drilling。

### 样式
- 使用 CSS Modules + CSS变量（设计令牌）；新增/修改样式一律优先复用 `global.css` 的 `--*` CSS变量（颜色、圆角、阴影等），禁止写魔法硬编码值；改动组件样式时同步清理该组件内已有的硬编码值。
- 必须兼顾亮色、深色主题；`data-theme='dark'` 模式下保证样式可读可用。
- 新增样式保持和现有项目响应式断点、布局风格对齐。

### Vite / 构建
- 路径别名统一使用 `@/`；路由做懒加载，大依赖做按需分包。
- 代码修改后必须保证 `tsc --noEmit` 编译通过；关注产物打包体积、重复依赖问题。

## 强制约束
- 非用户明确要求，**不要升级、新增npm依赖包**；如需新增/升级包，先说明理由，等待确认。
- 禁止无沟通大规模重构现有代码，大规模重构前输出重构计划，确认后再动手。
- 使用项目已有的eslint、prettier配置，保存代码保证lint无新增错误。
